# 区分数据面与控制面可用性目标

Agent Gateway、Channel Gateway、Agent Worker 和消息链路的数据面月度可用性目标为 99.95%，Admin API 与 Web Console 控制面目标为 99.9%。平台成功接收并持久化消息后计入数据面可用；外部模型和工具故障单独归因，但平台仍负责超时、重试、熔断和降级。单个 Pod 或节点故障不得中断服务，完整 SLO 依赖至少三个故障域，并必须由实际指标、告警和故障演练持续验证。
