# IM 回复使用可对账的至少一次投递

平台不承诺外部 IM Exactly Once，每个逻辑回复以稳定 delivery ID 进入持久状态机，每次尝试使用独立 attempt ID，并按外部会话序列化；Adapter 尽可能复用通道客户端幂等键、原消息更新和结果查询。发送前持久化，超时进入 OUTCOME_UNKNOWN 并优先对账而非立即新发，限流按通道指示退避，超期进入死信并以原 delivery ID 重放；通道能力不足时由租户策略权衡重复与丢失，默认避免重复高风险通知，所有尝试和最终状态进入审计与指标。
