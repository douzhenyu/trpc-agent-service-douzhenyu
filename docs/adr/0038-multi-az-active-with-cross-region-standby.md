# 地域内高可用并采用跨地域温备

主生产集群在单地域至少三个故障域内高可用，跨地域采用 Active/Standby 温备而非双活，以避免 Session 顺序、Kafka 所有权、数据库写入、IM 长连接和工具副作用的跨地域一致性风险。备用侧持续复制 WAL、对象、关键消息、配置和密钥引用但禁用业务消费与入口；切换先取得全局 Failover Lease 并 fencing 主集群，再提升存储、恢复位点、启用服务和切换入口，回切前重新追平验证。每季度演练并验证五分钟 RPO 与一小时 RTO。
