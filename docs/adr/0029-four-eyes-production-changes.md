# 高风险生产变更强制职责分离

Production Deployment、写入或高风险 Tool、Policy Bundle、数据分级、模型 Endpoint、生产密钥、存储迁移、保留与 Legal Hold、敏感导出等高风险生产变更采用四眼原则，发起人与审批人必须是不同平台用户且不得自批。Break-glass 仅用于故障处置，必须限定理由和短期有效期、自动通知、全量审计、事后复核并自动撤权；开发测试可按策略放宽，但生产核心职责分离默认不可关闭。
