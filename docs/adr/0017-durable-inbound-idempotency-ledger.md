# 使用持久入站幂等账本

每个外部事件在 PostgreSQL 中通过租户、通道绑定和稳定外部事件 ID 建立持久幂等记录；缺少稳定 ID 的协议使用版本化规范字段摘要。Channel Gateway 在同一事务中创建入站记录、唯一 Agent 执行和 Outbox，提交后才确认接收；重复事件复用原执行，相同键但 Payload 哈希不同的事件被隔离并告警。Redis 仅加速查询，内容删除后仍保留不含内容的紧凑去重墓碑，使 IM 重试、Kafka 重投和 Worker 恢复都不能重复创建执行。
