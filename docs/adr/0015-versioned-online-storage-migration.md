# 使用版本化配置档执行在线存储迁移

租户运行时后端通过版本化存储配置档迁移，流程依次经过准备、快照回填、Outbox/CDC 增量追赶、校验、原子切换、观察和完成，失败时进入显式回滚状态；业务代码不得无协调地双写新旧后端。源端在切换前保持权威，Session State 与 Summary 从不可变 Session Event 重建，活跃 Session 在安全边界排空，向量迁移使用版本化索引并在 Embedding 变化时重新向量化及影子验证；切换后源端保留只读观察期，以便在数据仍完整追赶时回滚。
