# 使用通道能力自适应流式回复

单聊默认以合并增量更新同一消息或卡片，群聊默认只发送处理中状态和最终结果；企业微信复用流式回复，飞书优先流式卡片，不按模型 Token 逐个调用 IM API。Adapter 按通道限制执行频控、Markdown 安全分段、超长内容 Artifact 化及附件安全检查，每次更新共享 delivery ID 保证幂等；不支持更新的通道退化为占位加最终消息。撤回不会回滚已发生副作用但可触发删除流程，取消执行停止增量并产生明确终态。
