# 使用不可变审计链路并默认保留一年

业务事务同步写入 Audit Outbox，异步落入 PostgreSQL 在线索引并生成按租户和日期哈希链及签名 Manifest，归档到启用 S3 Object Lock 的存储；审计只追加，更正以新事件引用旧事件，查询和导出自身也被审计。默认保留 365 天，租户可在 90 天至 7 年内配置并支持 Legal Hold；正文、秘密和工具参数默认不记录，明确开启的内容审计单独加密并缩短保留期，过期后执行可证明删除。
