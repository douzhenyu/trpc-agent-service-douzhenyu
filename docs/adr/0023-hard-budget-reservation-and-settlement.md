# 使用预算预留与结算硬门禁

平台在租户月度、Agent 应用每日和单次 Agent 执行三级执行预算门禁，并在每次模型或付费工具调用前按最坏允许用量预留，完成后按实际费用结算和释放。70% 与 90% 触发告警，100% 拒绝新预留，可按租户策略降级到更便宜的允许模型；已预留调用允许完成，预算状态无法确认时默认 fail closed，关键 Agent 可配置独立应急额度。所有变动写入不可变成本账本，价格表版本化以保证历史费用可复算。
