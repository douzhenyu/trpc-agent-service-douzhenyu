# 使用分层混合租户隔离

控制面元数据存放在共享 PostgreSQL 中，所有租户表通过 `tenant_id`、PostgreSQL RLS 和非超级用户连接强制隔离；运行时数据通过租户绑定的存储配置档路由，默认可在共享后端中使用强制命名空间隔离，高敏租户则可绑定独立 SQL、Redis、向量库、对象存储和专属 Worker Pool。该模型在企业内部多租户场景下兼顾隔离强度与运维成本，并要求每个租户使用独立的数据加密密钥引用。
